import { makeStyles } from '@material-ui/core/styles';
import { styled, alpha } from '@mui/material/styles';


export const SideBarContainer = styled('div')(({ theme })  => ({
    // backgroundColor: alpha(theme.palette.augmentColor('#E7F0C3'), 0.15),
    height: '100%'
}));